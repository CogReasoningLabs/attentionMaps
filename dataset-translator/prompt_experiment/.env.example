# Keep real values in .env (which should not be committed).
HF_token=hf_your_token
Gemini_api=your_gemini_api_key
# Optional: only needed when selecting the OpenAI provider in the UI.
OPENAI_API_KEY=your_openai_api_key
